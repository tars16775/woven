# Public origin used for robots.txt, sitemap.xml, the manifest and absolute metadata URLs. No trailing slash.
NEXT_PUBLIC_SITE_URL=https://woven.example

# Domain for the hello@/support@/press@ addresses shown on the site. Defaults to the host of NEXT_PUBLIC_SITE_URL.
NEXT_PUBLIC_MAIL_DOMAIN=woven.example
