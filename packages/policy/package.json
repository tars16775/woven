{
  "name": "@woven/policy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic action-risk policy: the model suggests, this decides.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@woven/schema": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
